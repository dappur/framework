{% extends 'base.twig' %}

{% import 'macros/global-config.twig' as configMacro %}

{% block title %}{{config['site-name']}} || Settings{% endblock %}

{% block stylesheets %}
<link href="https://cdn.datatables.net/1.10.15/css/dataTables.bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.datatables.net/responsive/2.1.1/css/responsive.bootstrap.min.css" rel="stylesheet">
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    Settings <small>Global</small>
                </h1>
                <ol class="breadcrumb">
                    <li>
                        <i class="fa fa-dashboard"></i> Dashboard
                    </li>
                    <li class="active">
                        <i class="fa fa-gear"></i> Settings
                    </li>
                    <li class="active">
                        <i class="fa fa-gears"></i> Global
                    </li>
                </ol>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-8">
                <form action="{{ currentRoute }}" method="post">
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            {{ settingsGrouped.name }}
                        </div>
                        
                        <div class="panel-body">
                                {% for item in settingsGrouped.config %}
                                    {% if item.type_id == 3 %}
                                        {{ configMacro.configTheme(item.name, item.description, "theme", item.value, themeList) }}
                                    {% elseif item.type_id == 2 %}
                                        {{ configMacro.configString(item.name, item.description, "string", item.value) }}
                                    {% elseif item.type_id == 1 %}
                                        {{ configMacro.configTimezone(item.name, item.description, "timezone", item.value, timezones, cfg.timezone) }}
                                    {% elseif item.type_id == 4 %}
                                        {{ configMacro.configBootswatch(item.name, item.description, "bootswatch", item.value, bsThemes) }}
                                    {% elseif item.type_id == 5 %}
                                        {{ configMacro.configImage(item.name, item.description, "image", item.value) }}
                                    {% elseif item.type_id == 6 %}
                                        {{ configMacro.configBoolean(item.name, item.description, item.value) }}
                                    {% else %}
                                        {{ configMacro.configDefault(item.name, item.description, item.value) }}
                                    {% endif %}
                                {% endfor %}
                        </div>
                    </div>

                    <div class="row" style="margin-top: 20px;">
                        <div class="form-group">
                            <div class="col-xs-12 col-sm-12 col-md-4 col-md-offset-4">
                                <button type="submit" class="btn btn-primary form-control">Save Settings</button>
                            </div>
                        </div>
                    </div>


                    {{ csrf() }}
                </form>
            </div>

            {% include 'inc/settings-controls.twig' with {'isPage': true} %}

        </div>
    </div>
{% endblock %}

{% block javascript %}
<script src="https://cdn.datatables.net/1.10.15/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.10.15/js/dataTables.bootstrap.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.1.1/js/dataTables.responsive.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.1.1/js/responsive.bootstrap.min.js"></script>

<!-- Page-Level Demo Scripts - Tables - Use for reference -->
<script>
$(document).ready(function() {
    
    $('#groups_table').DataTable({
        responsive: true,
        stateSave: true

    });

    if ($('#page').val() == 1) {
        $('.ispage').show();
    }else{
        $('.ispage').hide();
    }
});

$(document).on('change', '#page', function(){
    if ($(this).val() == 1) {
        $('.ispage').show();
    }else{
        $('.ispage').hide();
    }
});

$(document).on('change', '#add_type', function(){
    var type_id = $(this).val();
    if (type_id == 6) {
        $('#value_input').html('<select name="add_value" id="add_value" class="form-control" required="required">' +
            '<option value="0">False</option>' +
            '<option value="1" {% if requestParams['add_value'] == 1 %}SELECTED{% endif %}>True</option>' +
        '</select>');
    }else if (type_id == 5) {
        $('#value_input').html('<div class="input-group dm-input" data-name="add_value" data-value="{{ requestParams['add_value'] }}"></div>');
        DappurMedia.refreshInputs();
        /*
        $("#add_value").html(
            '<span class="input-group-btn">'+
                '<img id="'+inputName+'-thumbnail" src="'+inputValue+'" style="max-height: 30px; max-width: 30px;" />'+
            '</span>'+
            '<input type="text" name="'+inputName+'" id="'+inputName+'" class="form-control" value="'+inputValue+'">'+
            '<span class="input-group-btn">'+
                '<button type="button" class="btn btn-default image-preview" data-target="'+inputName+'"><i class="fa fa-eye"></i></button>'+
                '<button type="button" class="btn btn-default image-select" data-target="'+inputName+'"><i class="fa fa-picture-o"></i></button>'+
            '</span>'
        );
        */

    }else if (type_id == 4) {
        $('#value_input').html('<input type="text" name="add_value" id="add_value" class="form-control" value="{{ requestParams['add_value'] }}" placeholder="Value">');
    }else if (type_id == 3) {
        $('#value_input').html('<input type="text" name="add_value" id="add_value" class="form-control" value="{{ requestParams['add_value'] }}" placeholder="Value">');
    }else if (type_id == 2) {
        $('#value_input').html('<input type="text" name="add_value" id="add_value" class="form-control" value="{{ requestParams['add_value'] }}" placeholder="Value">');
    }else if (type_id == 1) {
        $('#value_input').html('<input type="text" name="add_value" id="add_value" class="form-control" value="{{ requestParams['add_value'] }}" placeholder="Value">');
    }else{
        $('#value_input').html('<input type="text" name="add_value" id="add_value" class="form-control" value="{{ requestParams['add_value'] }}" placeholder="Value">');
    }


});
</script>
{% endblock %}